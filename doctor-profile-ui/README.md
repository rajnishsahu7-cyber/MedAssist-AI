# MedAssist Doctor Profile UI

A responsive doctor profile built with React, Vite, Tailwind CSS, and Lucide icons.

## Run locally

```bash
npm install
npm run dev
```

Update `src/data/doctor.js` to display a real doctor and replace the `onBook` handler in `src/App.jsx` with the application appointment flow.
